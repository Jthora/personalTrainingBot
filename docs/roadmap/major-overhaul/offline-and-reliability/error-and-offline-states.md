# Error and Offline States

## States
- Offline with cache; Offline missing data; Cache stale; SW updating; Fetch error.

## Patterns
- Clear messaging with recovery steps (sync/go online/refresh).
- Text-first fallbacks; avoid blank states.

## Acceptance
- Each route has defined error/offline copy and UI; telemetry for errors and recoveries.
